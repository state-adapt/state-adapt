import { eslintPluginSpaghetti } from './eslint-plugin-spaghetti';

describe('eslintPluginSpaghetti', () => {
  it('should work', () => {
    expect(eslintPluginSpaghetti()).toEqual('eslint-plugin-spaghetti');
  });
});
